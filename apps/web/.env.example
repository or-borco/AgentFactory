DATABASE_URL=postgres://agentfactory:agentfactory@localhost:5432/agentfactory
REDIS_URL=redis://localhost:6379

# GitHub App used by the Connections feature (apps/web/src/server/github-app.ts). Register one
# via /api/admin/github-app/register (manifest flow — no manual GitHub dashboard form-filling)
# and copy the values it returns here.
GITHUB_APP_ID=
GITHUB_APP_SLUG=
GITHUB_APP_PRIVATE_KEY=
